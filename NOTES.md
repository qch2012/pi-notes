# Notes

- User is a heavy daily user of Claude-Code-style terminal coding agents already — skip conceptual intros to the category (slash commands, context files, sessions), focus lessons on Pi's specifics and deltas from what they already know.
- Goal is adopting Pi as a daily driver, not evaluation or extension-building (for now) — bias lessons toward "do this for real, today" over theory.
- This workspace directory (`/home/andy/projects/pi`) doubles as an actual coding project scaffold (see `CLAUDE.md`/`AGENTS.md`/`docs/agents/`, set up separately) — no conflict, just worth knowing both live here.
